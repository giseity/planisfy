# Local Font Data

Run `scripts/fonts-dev.sh download` from the repository root to hydrate this
directory with the Open Sans TTF used by local glyph serving. The TTF files are
ignored by git and mounted into Martin at `/fonts`.

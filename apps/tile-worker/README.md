# Tile Worker

Placeholder app. It is not wired into the current runtime and has no package scripts in this repository.

Current public tile delivery is handled by `apps/api` with PMTiles/storage reads and Martin fallback.

Product gaps belong in `PLANISFY_ROADMAP.md`.

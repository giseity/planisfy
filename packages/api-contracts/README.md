# @planisfy/api-contracts

Shared Zod schemas and TypeScript contracts for Planisfy API request and response shapes.

This package must stay runtime-light: no database, storage, queue, or app imports.

Commands: `pnpm --filter @planisfy/api-contracts test`, `check-types`, `lint`.

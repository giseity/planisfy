# @planisfy/database

Drizzle schema, migrations, relations, database client, and shared server-side data helpers.

Important config: `DATABASE_URL`.

Commands: `pnpm --filter @planisfy/database db:generate`, `db:migrate`, `db:push`, `db:studio`, `check-types`, `lint`.

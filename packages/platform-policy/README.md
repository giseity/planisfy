# @planisfy/platform-policy

Deployment-mode capability policy for `self_host` and `managed`.

No runtime service config; callers pass deployment mode values.

Commands: `pnpm --filter @planisfy/platform-policy test`, `check-types`, `lint`.

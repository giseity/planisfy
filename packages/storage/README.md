# @planisfy/storage

Server-side storage abstraction for local filesystem and S3/R2-compatible object stores.

Important config: `STORAGE_PROVIDER`, local storage variables, S3 variables, and R2 variables.

Commands: `pnpm --filter @planisfy/storage check-types`, `lint`.
